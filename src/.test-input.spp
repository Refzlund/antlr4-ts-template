                      				// Max count, excluding it, makes SomeClass staticly accessible
class SomeClass someClass[10] {
	int client = UserFromClient(this)
	int val = 25
	int test = 100
	private int privateField = 125
	int noInitial // becomes 0
	int fnInitial = SomeFunction(this.client)
 
	SomeClass(int index) {
		this.val = 69
	}

	void SomeFunc() {
		// referenced as someClass.*()
		this.client
	}

	static void SomeStaticFunc() {
		// there's no "this", referenced as SomeClass.*(), instead of someClass.*()
	}

	[OnPluginStart]
	static void ForwardedFn() {
		
	}
	
	[OnPluginStart_Post]
	void ForwardPost() {
		// Auto-deletes JSONObject handle
		json(value) {
			value = {
				"some": "value"
			}

			httpRequest.Post(value, "/some-route")
		}
	}

	[OnPluginStart_Complicated]
	bool ComplicatedForward(int someVal) {

	}
}